version: 1.0.{build}
image:
- Ubuntu

environment:
  matrix:
    - ARCH: arm32v7
    - ARCH: arm64v8
    - ARCH: amd64

build_script:
  - ps: ./build.ps1

test_script:
  - ps: ./test.ps1

deploy_script:
  - ps: ./deploy.ps1
